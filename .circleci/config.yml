# Use the latest 2.1 version of CircleCI pipeline process engine. See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1
# Use a package of configuration called an orb.
orbs:
  # Declare a dependency on the welcome-orb
  welcome: circleci/welcome-orb@0.4.1
# Orchestrate or schedule a set of jobs
workflows:
  # Name the workflow "welcome"
  welcome:
    # Run the welcome/run job in its own container
    jobs:
      - welcome/run
// definir led amarelo como saída digitalWrite (vdPin, HIGH); // ligar led verde} void loop () {if (statusVD == 1) {if ((millis () - evento)> = vdTempo) {digitalWrite (vdPin , BAIXO); statusVD = 0; digitalWrite (amPin, HIGH); statusAM = 1; evento = millis (); }} if (statusAM == 1) {if ((millis () - evento)> = amTempo) {digitalWrite (amPin, LOW); statusAM = 0; digitalWrite (vmPin, HIGH); statusVM = 1; evento = millis (); }} if (statusVM == 1) {if ((millis () - evento)> = vmTempo) {digitalWrite (vmPin, LOW); statusVM = 0; digitalWrite (vdPin, HIGH); statusVD = 1; evento = millis (); }}} = amTempo) {digitalWrite (amPin, LOW); statusAM = 0; digitalWrite (vmPin, HIGH); statusVM = 1; evento = millis (); }} if (statusVM == 1) {if ((millis () - evento)> = vmTempo) {digitalWrite (vmPin, LOW); statusVM = 0; digitalWrite (vdPin, HIGH); statusVD = 1; evento = millis (); }}} = amTempo) {digitalWrite (amPin, LOW); statusAM = 0; digitalWrite (vmPin, HIGH); statusVM = 1; evento = millis (); }} if (statusVM == 1) {if ((millis () - evento)> = vmTempo) {digitalWrite (vmPin, LOW); statusVM = 0; digitalWrite (vdPin, HIGH); statusVD = 1; evento = millis (); }}}
